import 'package:flutter/material.dart';

class PrepYear extends StatelessWidget {
  const PrepYear({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("prep year"),
      ),
    );
  }
}
